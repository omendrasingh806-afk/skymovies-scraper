services:
  - type: web
    name: skymovieshd-gdflix-scraper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
      - key: MAX_WORKERS
        value: "10"
